# 英国配电部件补标规范 v1

2026-08-27。此文件是后续开发标注规范，不代表27张图已经标注完成，也不启动新的训练。

## 当前交付

补标清单为新报告中的 `material_review_queue.json`：全部27张原图都列为任务，包含20张监督模型在分数0.25下没有输出的图。7个机器候选框仅用于辅助定位，其中1个为绝缘子候选。`reviewed_objects` 均为空；材质均为 `null`。模型无框不等于图中无部件。

原始图像、作者、来源页、许可及SHA256逐图保留。不得只审核有候选框的图，不得将机器候选直接导入为训练真值。视觉提示所用的一张参考图另有5个助手临时圈定的框；这不是人工完成标注，也不进入26张目标的结果。

## 对象与属性分开记录

| 字段 / 类别 | 标注单位 | 不应混淆 |
|---|---|---|
| `pole` | 一根杆体的可见轮廓外接框；注明截断和遮挡 | 不把整个杆上设备群都算作杆体 |
| `crossarm` | 一个明确的横担主梁；同杆多梁分别标注 | V形支撑、线缆、变压器、材料标签不是横担主梁 |
| `insulator` | 一串完整绝缘子或一个独立安装的绝缘子；排除连接金具 | 单个伞裙/盘片与整串不可混为同一单位 |
| `material` | 绝缘子实例的独立属性：glass / porcelain / polymer / unknown | `polymer` 不等于 porcelain；颜色、形状或模型分数不能单独证明材质 |
| `pole-top` | 暂不作为训练类别；先与使用者明确顶端区域范围 | 几何推导ROI不是有检测置信度的真实类别 |
| `steelwork` | 暂不重命名任何现有类别；需定义覆盖哪些金属构件并独立审核 | 木横担不是steelwork；仅识别横担形状不能证明它是钢制 |

框用原图像素 `[x1,y1,x2,y2]`，限制在图像范围内。不要用放大显示的屏幕坐标。只框可见范围，另存 `truncated` / `occluded`；不要猜测图外或完全遮挡部分。保留不同类别的嵌套框。

EPRI官方多边形和派生框保持原样；本规范不追溯修改已完成实验的GT。若以后协调数据集的标注定义，应创建新版本并保留变更记录。

## 每张图的复核步骤

1. 先看完整原图，再放大所有杆上部件。确认候选类型，删除误检，补上所有可见漏检。没有目标也必须明确记录为人工确认的空图。
2. 统一实例边界和标注单位。无法区分两个重叠部件时标记困难区域，不凭空增加实例。
3. 材质证据不足时用 `unknown`，并记录原因，如像素不足、遮挡或缺乏独立产品/资产资料。`null` 表示尚未审核，`unknown` 表示审核后仍无法判定，二者不可混用。
4. 记录审核者、审核时间、类别、框、材质及依据。材质最好由有电力设备经验的人二次审核；分歧单独保存，不用多数模型输出替代依据。
5. 记录可靠的资产/航次ID。不能仅因文件名相似、同地点或外观近似就认定同一资产；未知身份保留未知。

## 后续评估边界

这27张图已经用于查看多个模型输出，全部属于开发资料；不能事后称为未触碰测试集。真正的英国验证需另外保留并核实独立资产，覆盖近景、远景、角度、遮挡和无目标图。

在材质标签和独立资产数足够前，不训练或报告“玻璃/瓷高准确率”。后续应分别评估部件定位、真实裁剪的材质分类、自动框的端到端位置+材质结果，并报告拒判覆盖率。缺陷与危险等级不在当前标签体系中。

当前没有已授权排队的新训练。补标审核与新增数据决策完成后，再固定下一次协议与预算。
