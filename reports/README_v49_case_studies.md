# v4.9 case-study explorer

The Pages case-study explorer is generated from existing repository evidence rather than a separate manually maintained metrics table.

Inputs used at build time:

- `reports/v3_8_final_holdout_summary.json` for frozen final-holdout metrics;
- `reports/v3_9_morphology_shift.md` for the analysis-only morphology diagnosis;
- `reports/v4_2_preadaptation_summary.json` for the unchanged-method development diagnostic;
- `reports/v4_3_morphology_diversity_summary.json` for the controlled YOLO26 ablation;
- `reports/v4_4_pixel_review.json` and `data/v4_annotations/material_reference_v44.json` for pre-inference source/material semantics;
- the freshly reproduced `reports/v4_6_siglip2_material_crop_results.json` for adaptive SigLIP2 method results;
- `_site/assets/dataset/manifest.json` for the 16 source-image roles and provenance generated by v4.8.

The explorer keeps training, validation, adaptive development, retired final holdout, unspent holdout and scale-stress evidence separate. In particular, `POS_7630781` remains an unspent detection holdout and is displayed with `NOT RUN` rather than being consumed to create a more attractive portfolio result.

No condition, corrosion, defect, mechanical-integrity, reliability or safety-risk claim is added by v4.9.
